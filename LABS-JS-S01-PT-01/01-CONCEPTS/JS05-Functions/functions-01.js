// FUNCTIONS
// DRY: Dont Repeat Yourself
// Classes
// Interfaces
// packages
// loop

// function, declaring
function a() {
	console.log( "Using the a function"  );
}
// execute, call, invoke, run, evaluate, using
// a();

function b( x, y ) {
	console.log( x + y );
}
// b( 1, 2 );
// b( "1", "2" );
// b( 1, "2" );

function c( z ) {
	z * 2;
	// return z * 2;
}
var r = c( 1 );
console.log( r );


